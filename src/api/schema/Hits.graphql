input HitInput {
    category: String!
    tags: [String!]!
}

type CategoryHits {
    category: String!
    tags: [String!]!
}

extend type Query {
    alphaHitsPopular(categories: [String!]!): [CategoryHits!]
}

extend type Mutation {
    alphaHitsAdd(hits: [HitInput!]!): String
}
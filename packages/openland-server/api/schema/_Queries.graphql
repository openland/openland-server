type Query {

    alphaInvites: [Invite!]
    alphaInviteInfo(key: String!): InviteInfo
    alphaAppInvite: String!
    alphaAppInviteInfo(key: String!): AppInviteInfo
    alphaInvitesHistory: [InviteHistotyInfo!]

    alphaRefreshTask(id: ID!): Task!

    me: User
    myProfile: Profile
    myProfilePrefill: ProfilePrefill

    users(query:String!): [User!]!

    user(id: ID!): User!

    ping: String!
}
union GlobalSearchEntry = Organization | User | SharedRoom

extend type Query {
    alphaGlobalSearch(query: String!): [GlobalSearchEntry!]!
    featuredGroups: [SharedRoom!]
    featuredCommunities: [Organization!]
}
input Event {
    id: String!
    event: String!
    params: String
    time: Date
}

enum EventPlatform {
    Android,
    iOS,
    WEB
}

extend type Mutation {
    track(did: String!, events: [Event!]!, platform: EventPlatform, isProd: Boolean): String!
}
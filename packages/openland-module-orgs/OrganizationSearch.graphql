extend type Query {
    alphaOrganizations(
        query: String,
        prefix: String,
        sort: String,
        first: Int!,
        after: String,
        page: Int
    ): OrganizationsConnection!
    alphaOrganizationByPrefix(
        query: String,
    ): Organization
    alphaComunityPrefixSearch(
        query: String,
        sort: String,
        first: Int!,
        after: String,
        page: Int
    ): OrganizationsConnection!
}
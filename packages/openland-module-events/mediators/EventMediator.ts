import { Context } from '@openland/context';
import { Event } from '../Definitions';

export class EventMediator {
    postEvent = async (ctx: Context, uid: number, event: Event) => {
        return null;
    }

    refreshOnline = async (ctx: Context, uid: number) => {
        //
    }
}